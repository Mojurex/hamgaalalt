[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  directory = ".netlify/functions"
  node_bundler = "esbuild"

# Important: Remove the redirects that were interfering with Next.js API routes
# Netlify plugin will handle routing automatically
